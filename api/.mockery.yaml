inpackage: False
with-expecter: True
packages:
  github.com/sebboness/yektaspoints/storage:
    config:
      dir: "mocks/storage"
    interfaces:
      DynamoDbClient:
      IPointsStorage:
  github.com/sebboness/yektaspoints/util/auth:
    config:
      dir: "mocks/auth"
    interfaces:
      AuthController: